# Claude AI – Laravel Birds Ecommerce Project Instructions
You are the AI engineer for this Laravel project. Follow all rules below for every request, every file, and every code generation task.

---

# 🟩 PROJECT DESCRIPTION
We are building a **Birds Ecommerce Website** using Laravel.  
Frontend is already built in **HTML/CSS/JS** generated by Stitch.  
Your job is to convert everything into a **fully dynamic Laravel application** following best practices.

---

# 🟦 GENERAL RULES
- Always write clean, maintainable Laravel code.
- Use Laravel 10+ conventions.
- NEVER modify vendor or framework files.
- Always create missing directories when needed.
- When I paste HTML, automatically convert it to Blade.
- Use components wherever possible.
- Use @extends, @section, @yield correctly.
- Never inline long scripts or CSS inside views.

---

# 🟨 FOLDER STRUCTURE RULES
Always follow this structure:

resources/views/
layouts/
app.blade.php
admin.blade.php
components/
navbar.blade.php
footer.blade.php
product-card.blade.php
category-card.blade.php
button.blade.php
breadcrumb.blade.php
pages/
home.blade.php
shop.blade.php
product.blade.php
cart.blade.php
checkout.blade.php
about.blade.php
contact.blade.php
wishlist.blade.php
profile.blade.php
orders.blade.php

public/
css/
js/
images/

---

# 🟥 ASSETS RULES
- Move all CSS to **public/css**
- Move all JS to **public/js**
- Move all images to **public/images**
- In blade use:
```<link rel="stylesheet" href="{{ asset('css/style.css') }}"> <script src="{{ asset('js/main.js') }}"></script>```

# ROUTING RULES
All views/pages must have proper named routes.
Example:

Route::get('/', [HomeController::class, 'index'])->name('home');
Route::get('/shop', [BirdController::class, 'index'])->name('shop');
Route::get('/bird/{slug}', [BirdController::class, 'show'])->name('bird.show');
Route::get('/cart', [CartController::class, 'index'])->name('cart');
Route::get('/checkout', [CheckoutController::class, 'index'])->name('checkout');

Route names must be consistent and camelCase.


# BLADE TEMPLATE RULES
For every HTML file:

Convert it into a Blade file using:

@extends('layouts.app')

@section('content')
    <!-- page content here -->
@endsection

Extract all repeated UI into components:
Navbar → <x-navbar />
Footer → <x-footer />
Product card → <x-product-card :bird="$bird" />
Category card → <x-category-card :category="$category" />
Buttons → <x-button />

Do NOT duplicate code.


# LARAVEL COMPONENTS RULES
Components must be created inside:

resources/views/components/

Use props properly, e.g.:

@props(['bird'])


# MODELS & DATABASE RULES
Required models:
    Bird
    Category
    User
    Order
    OrderItem
    Review
    CartItem
    Setting

Required relationships:
    Category hasMany Birds
    Bird belongsTo Category
    Bird hasMany Reviews
    User hasMany Orders
    Order hasMany OrderItems
    User hasMany CartItems

Always include:
    fillable fields
    casting
    relationships
    slug generation (for birds)


# CONTROLLERS RULES
Generate the following controllers:
    HomeController
    BirdController
    CategoryController
    CartController
    CheckoutController
    OrderController
    ReviewController

Controllers must:
    Pass dynamic data to views
    Use Eloquent properly
    Handle cart logic
    Handle checkout flow
    Validate inputs
    Return clean blade pages


# ADMIN PANEL RULES
Create a simple admin panel with CRUD for:
    Birds
    Categories
    Orders
    Users
    Settings

Use:
    Admin controllers like app/Http/Controller/Admin
    Admin views like resources/views/admin
    Admin layout → layouts/admin.blade.php


# WHEN USER PASTES HTML
When the user pastes any HTML:
You must automatically:
    Detect the page (home, shop, product, etc.)
    Move CSS/JS/image references to asset() helpers
    Convert into proper Blade structure
    Extract components if repeated
    Save file in correct folder under pages/
    Tell user where the file is placed
    Suggest dynamic data bindings

Never ask again — just convert.


# WHEN USER REQUESTS “MAKE DYNAMIC”
Automatically:
    Load data from models
    Replace static HTML with loops
    Add controllers
    Add route() usage
    Add database fields
    Use @foreach for product lists
    Add dynamic slugs, URLs, images

# AUTO-LINTING RULE
Whenever code is generated:
    Follow PSR-12 formatting
    Use modern PHP/Laravel syntax
    Keep code clean and readable

# FILE OPERATIONS RULE
You are allowed to:
- Create new files
- Modify existing files
- Replace file contents
- Create directories
- Move files
- Refactor file structure

Every time you generate Blade, controllers, models, or migrations,
you MUST create the actual file inside the Laravel project.


# FINAL RULE
Always respond with the full code solution, never partial, and always inside proper code blocks.
This file defines your permanent behavior for this project.
Always follow these rules strictly.


---

# 🎉 **Your claude.md file is ready!**

Now Claude will:

✅ Automatically convert HTML → Blade  
✅ Build controllers automatically  
✅ Build database automatically  
✅ Build admin panel  
✅ Use components  
✅ Keep file structure correct  
✅ Make every page dynamic  
✅ Stop asking repetitive questions  

Everything becomes **fully automated**.

---

# Want me to create:
✅ Full Laravel folder structure  
✅ Starter Blade layouts  
✅ Controllers skeletons  
✅ Models + migrations (all files generated)  

I can generate these instantly.
